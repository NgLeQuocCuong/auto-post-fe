import Search from 'antd/es/transfer/search';
import {FC, memo} from 'react';

const Search: FC = memo(
    () => {
        return (
            <div>

            </div>
        )
    }
)

Search.displa